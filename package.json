{
  "name": "snipcopy-extension",
  "version": "0.2.0",
  "private": true,
  "description": "Build tooling for SnipCopy's on-device OCR engine (PaddleOCR via onnxruntime-web).",
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "onnxruntime-web": "^1.27.0",
    "ppu-paddle-ocr": "^6.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
